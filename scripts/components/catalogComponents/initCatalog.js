// Инициализация секции каталога
import { createCatalogSection } from './catalogComponent.js';

document.addEventListener('DOMContentLoaded', () => {
    createCatalogSection('cards__container'); // Убедитесь, что ID контейнера совпадает с вашим HTML
});
